# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# Canonical product behavior

This app is the current SafeSpread iPhone app. Rectangle setup accepts entered
dimensions only. Do not restore the retired Walk corners, Corner A, Corner B,
`CAPTURE_CORNER_A`, `CAPTURE_CORNER_B`, or `rectangleMode: 'walked'` workflow
from historical plans, commits, branches, worktrees, or generated builds.

The emergency Stop control must remain fixed, full-width, and at the top of the
screen. Build and deploy only from the latest `origin/main`.
